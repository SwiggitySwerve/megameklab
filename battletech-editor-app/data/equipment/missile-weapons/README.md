# Missile Weapons Data Modularization

This directory contains modularized data files for each missile weapon family (LRM, SRM, Streak, ATM, MML, Thunderbolt, MRM).

- Each file exports all weapons in that family.
- `index.ts` re-exports all families and provides an aggregated `MISSILE_WEAPONS` array. 